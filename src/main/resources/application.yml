# Public Configuration
# 公共配置文件
spring:
  application:
    name: user-center-demo # 自己项目的项目名

  # Database Configuration
  # 数据库配置
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/user_center_demo # 需要修改成自己的数据库链接
    username: root # 需要修改成自己的数据库用户名
    password: Wenrh2004 # 需要修改程自己的数据库密码
  # Session Configuration
  session:
    timeout: 3000

---
# Server Configuration
# 服务配置
server:
  port: 8080
  servlet:
    context-path: /api # /api 表明后端服务 API 接口
